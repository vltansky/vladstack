interface:
  display_name: "Octocode Research"
  short_description: "Deep code exploration and analysis"

dependencies:
  tools:
    - type: "mcp"
      value: "octocode"
      description: "Octocode MCP server for code search and analysis"

policy:
  allow_implicit_invocation: true
